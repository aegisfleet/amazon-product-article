1. Add a source entry to the `sources` array in `data/investigations/B0C654Z5PC.json`.
2. Update the `supportingSourceIds` in `userStories` and `claims` to reference the newly added source id.
3. Validate the JSON structure using `scripts/validate_artifact.py`.
4. Submit the changes.
