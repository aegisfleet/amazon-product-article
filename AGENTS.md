# AIエージェント開発ガイドライン

このドキュメントは、Amazon商品調査システムに携わるAIコーディングエージェントのためのベストプラクティスと必須手順をまとめたものである。

## 1. 必須の検証手順

いかなるタスクも、プロジェクト全体の健全性を確認するまでは完了とみなしてはならない。以下のコマンドが正常に終了することを必ず確認する：

- **Lint**: `npm run lint`
  - ESLintエラーが0件であることを確認する（警告は許容される）。
  - よくあるエラー例：
    - `no-base-to-string`: オブジェクトを文字列化すると `[object Object]` になる
    - `no-useless-escape`: 正規表現内の不要なエスケープ文字（例：文字クラス `[...]` 内の `\(` や `\)`）
    - `no-unsafe-member-access`: `any` 型へのメンバーアクセス
- **ビルド**: `npm run build`
  - TypeScriptのコンパイルエラーがないことを確認する。
- **テスト**: `npm test`
  - すべてのユニットテストおよびプロパティベースのテストがパスすることを確認する。
  - プロンプトやコアロジックを修正した場合は、プロパティテストが依然として通ることを特に確認する。
- **アーティファクト検証**: `python scripts/validate_artifact.py <生成したJSONファイル>`
  - 調査結果や推薦リストの生成・修正を行った場合は、必ず実行する。
  - 構造の正当性、メートル法（単位）の遵守、およびリンク切れ（Amazonのソフト404を含む）がないことを確認する。

> **重要**: GitHub Actions CIはLintエラーがあると失敗する。実装完了前に必ず `npm run lint` を実行し、エラーが0件であることを確認する。

## 2. テスト駆動開発 (TDD)

可能な限りTDD（テスト駆動開発）のアプローチを採用すること。

1. **まずテストを書く**: `src/**/__tests__/` に期待される挙動を定義するテストケースを作成、または更新する。
2. **実装・修正**: テストをパスさせるために必要な最小限のコードを書く。
3. **リファクタリング**: テストが通る状態を維持しながら、コードを整理する。

この手法は、正規表現やプロンプトを多用するこのシステムにおいて、エッジケースを捉えるためのプロパティベーステスト（`fast-check`を使用）において特に重要である。

## 3. プロンプトエンジニアリングと安全性

このシステムは、`src/jules/JulesInvestigator.ts` などに含まれる緻密に構成されたプロンプトに大きく依存している。

- **構文破壊の回避**: 巨大なテンプレートリテラルを編集する際は、バックティックや波括弧の対応を壊さないよう細心の注意を払う。
- **フレーズの維持**: プロパティテストは、特定の指示やキーワードが含まれていることをチェックしている。プロンプトを整理・洗練させる際は、検証を壊さないよう慎重に行う。
- **検証の実行**: プロンプト生成クラスを変更した後は、必ず関連するテストスイート（例：`npm test src/jules/__tests__/JulesInvestigator.property.test.ts`）を実行する。

## 4. セキュリティと認証情報

Amazon Creators APIの認証情報の取り扱いには、最優先でセキュリティを確保すること。

- **ログ出力の禁止**: `AMAZON_CREATORS_APPLICATION_ID`, `AMAZON_CREATORS_CREDENTIAL_ID`, `AMAZON_CREATORS_CREDENTIAL_SECRET` をコンソール、ログ、ファイル、またはコミットメッセージに出力しないこと。
- **環境変数の利用**: 秘密情報には `process.env` を通じてアクセスすること。キーをハードコードしたり、平文のファイルに保存したりしないこと。
- **エラーメッセージ**: API失敗時のエラーハンドリングで機密情報が漏洩しないようにすること。

## 5. リポジトリの構造

混乱を避けるため、以下のディレクトリ構造を意識すること：

- `/scripts/`: Creators APIとの直接的なやりとりやデータ収集のためのPythonスクリプト。
- `/src/scripts/`: メインアプリケーションのTypeScript CLIエントリポイント。
- `/data/`: 商品データや調査結果が格納される。明示的な指示がない限り、巨大な研究用ファイルや一時ファイルをコミットしないこと。

## 6. カテゴリグループ管理

カテゴリを親グループに整理する際は、**`data/categorygroups.json` のみ編集する**。カテゴリの追加や修正の際は、各親グループ内でのソート順が**Unicodeコードポイント順（JavaScriptのデフォルトのsort順）**になるように維持する。

> [!IMPORTANT]
> **ソートの優先順位**:
> 1. 英数字 (`0-9`, `A-Z`)
> 2. ひらがな
> 3. カタカナ
> 4. 漢字
>
> 読み（あいうえお順）ではなく、文字コード順で判定される。迷った場合は `npm run sort:categories` を実行して自動整理する。

| ファイル | 用途 |
|---|---|
| `data/categorygroups.json` | カテゴリ定義の唯一の情報源（Hugo テンプレート・フロントエンド共用） |
| `static/js/category-dropdown.js` | 実行時にJSONを動的読み込み（カテゴリデータのハードコード不要） |

> **Note**: `static/data/categorygroups.json` はビルド時に `npm run prebuild:hugo` で自動生成されます（Gitには含まれません）。
> 同時に Hugo 用の `data/categories.yml` および `content/parent-category/*.md` も自動生成され、親カテゴリページの動的生成に利用される。

### 6.1 新規親カテゴリの追加

`data/categorygroups.json` に新しい親カテゴリを追加する際は、以下の手順を必ず実行する：

1. **`data/categorygroups.json` を編集する**
   - `categoryGroups` 配列に新しいオブジェクトを追加します。オブジェクトには以下のプロパティを指定します：
     - **必須**: `name`（親カテゴリ名）、`slug`（URLスラッグ）、`children`（子カテゴリ名の配列）
     - **任意**: `visible`（表示フラグ、デフォルト `true`）、`priority`（ソート優先度、デフォルト `999`）

2. **ビルドして確認する**
   - `npm run prebuild:hugo` を実行すると、以下のファイルが**常に**生成・更新される：
     - `data/categories.yml`（Hugo用データ）
     - `static/data/categorygroups.json`（フロントエンド用データ）
     - `content/parent-category/*.md`（親カテゴリ用ページ本体）
   - ただし、商品数が0のカテゴリおよび親カテゴリは、フロントエンドのナビゲーションや一覧では**非表示**扱いとなる（例外：「その他」カテゴリは商品数0でも表示される）。
   - `hugo server` で新しい親カテゴリページの表示・ナビゲーション反映を確認する。

## 7. コミュニケーション

- 積極的に提案を行いつつも、既存のパターンに従う。
- 要件が曖昧な場合は、実装を進める前にユーザーに確認を行う。
- 変更内容は `walkthrough.md` に明確に記録する。

## 8. Creators API 調査ツールの使用

商品調査時に以下のスクリプトを使用できる。**これらのスクリプトは編集せずに使用すること。**

| スクリプト | 用途 | 出力先 |
|---|---|---|
| `scripts/creators_get_item.py` | 商品詳細取得 | `tmp/product_info.json` |
| `scripts/creators_search_items.py` | 競合商品検索 | `tmp/search_results.json` |

### 使用例

```bash
# 商品詳細の取得
python scripts/creators_get_item.py B09BZ59Y51

# 競合商品の検索（SearchIndex指定可能）
python scripts/creators_search_items.py "枕カバー フランネル" --search-index HomeAndKitchen
```

### 利用可能な SearchIndex (Japan)
- `All` (デフォルト/フォールバック用)
- `Electronics` (家電・カメラ・AV機器)
- `HomeAndKitchen` (キッチン・日用品・家具・ペット)
- `HealthPersonalCare` (ドラッグストア/漢方・ビューティー)
- `GroceryAndGourmetFood` (食品・飲料・お酒)
- `Fashion` (服＆ファッション小物)
- `ToolsAndHomeImprovement` (DIY・工具)
- `Appliances` (大型家電)
- `Toys` (おもちゃ)
- `Books` (本)

> **注意**: `tmp/` ディレクトリは `.gitignore` 対象です。調査結果ファイルはコミットしないでください。

## 9. UI/UX 実装ガイドライン

### 商品カードとヒーローカードの同期
商品の魅力を伝えるためのUIコンポーネントは、以下の3箇所で整合性を保つように実装する。

| 場所 | 実装ファイル | 役割 |
|---|---|---|
| **商品詳細 (ヒーローカード)** | `layouts/partials/product-hero.html` | 記事冒頭のメインカード (Hugo partial, データはArticleGeneratorがFront Matterへ出力) |
| **子カテゴリ一覧 (インライン)** | `layouts/_default/list.html` | 詳細・子カテゴリページのリスト項目 |
| **親カテゴリ一覧 (インライン)** | `layouts/_default/parent-category.html` | 親カテゴリページのリスト項目 (**注意**: カード定義がインラインに含まれるため同期必須) |
| **検索/ウィジェット (カード)** | `layouts/partials/product-card.html` | その他の箇所で使われるカード部品 |

**重要なルール**:
今回のように「スペック表示」や「バッジ」、「価格表示」などの新しい要素を追加する場合は、**これら4ファイルすべて**に対して実装を行い、ユーザー体験を統一する。
以前は TypeScript で HTML を生成していたが、リファクタリングにより **全て Hugo テンプレート側で HTML を生成する** アーキテクチャに統一された。`ArticleGenerator.ts` は Front Matter へのデータ出力のみを担当する。

## 10. 開発環境

- **ローカルサーバーURL**: `http://localhost:1313/`
  - Hugoサーバーを起動 (`hugo server`) した際のデフォルト確認用URLである。

## 11. コードレビューのガイドライン (Review Guidelines)

- **`data/` ディレクトリ配下のファイルはレビュー対象外とする。**
  - 商品調査結果、キャッシュ、メタデータなどの自動生成ファイルが含まれるため、コード品質のレビューは不要である。
  - これらのデータは毎日新しい情報で上書きされるため、既存データと大きく乖離する場合（例：おすすめ商品の総入れ替えや価格の変動など）があるが、それは仕様であり問題ない。既存データとの整合性よりも、「本日の最新情報」であることを優先する。
  - **ユーザーストーリーの削除**: データの更新時に、過去に記載されていたユーザーストーリーの根拠（レビューや記事などのエビデンス）が見つからない場合は、根拠のないストーリーは不要なため、そのストーリーを削除する。
  - **異常な割引率の制限**: 割引率が 80% や 90% など異常に高い商品は、元の価格を不正に詳しく釣り上げている（二重価格表示）可能性が高いため、安易に調査・推奨の対象としない。
